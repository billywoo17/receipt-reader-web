categorychart(dataArray){
    let results = []
    dataArray.forEach(function(d){
      if results
    results.push({value: d.total, label: d.cat_name})
    })
    return results;
  }


----

TAKES IN